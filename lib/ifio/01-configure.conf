[apply configure]
./configure --prefix "${PREFIX}" --host "${HOST}" --build "${BUILD}"
make -j$CORES
make install

[assign configure]
PREFIX=/usr
CORES="$(nproc)"
HOST=x86_64-linux-gnu
BUILD=x86_64-linux-gnu

[assess configure]
test -f ./configure
