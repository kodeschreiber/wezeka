[apply autogen]
./autogen.sh --prefix "${PREFIX}" --host "${HOST}" --build "${BUILD}"
make -j$CORES
make install

[assign autogen]
PREFIX=/usr
CORES="$(nproc)"
HOST=x86_64-linux-gnu
BUILD=x86_64-linux-gnu

[assess autogen]
test -f ./autogen.sh
